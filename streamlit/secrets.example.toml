# Copy to streamlit/.streamlit/secrets.toml (or ~/.streamlit/secrets.toml) and edit.
# Auth uses Application Default Credentials — run `gcloud auth application-default login`
# with a read-only principal (bigquery.dataViewer on voy_analytics + bigquery.jobUser).

gcp_project   = "zains-gcp"
marts_dataset = "voy_analytics"   # where dbt built the models
snapshot_date = "2024-08-16"
