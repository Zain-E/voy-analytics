# dbt
target/
dbt_packages/
logs/
.user.yml

# python / streamlit
.venv/
.venv-dbt/
streamlit/.venv/
__pycache__/
*.pyc

# dashboard query cache (parquet snapshots — regenerated on first run)
.voy_cache/
**/.voy_cache/

# streamlit secrets — never commit (match at any depth)
secrets.toml
**/secrets.toml
.streamlit/secrets.toml

# creds — never commit
*.json
!package*.json
.env
*.local

# local scratch — backup zips, stale lock files
_to_delete/

# os
.DS_Store
